<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Projectttt">

        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LoginActivity" />
        <activity android:name=".DaftarActivity" />
        <activity android:name=".BerandaActivity" />
        <activity android:name=".LaporActivity" />
        <activity android:name=".LaporanTerkirimActivity" />
        <activity android:name=".StatusActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".EdukasiActivity" />
        <activity android:name=".FaqActivity" />

        <receiver
            android:name=".receiver.NotifikasiReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.projectttt.ACTION_LAPORAN_TERKIRIM" />
                <action android:name="com.example.projectttt.ACTION_SOS_ACTIVATED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>